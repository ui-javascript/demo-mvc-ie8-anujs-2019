// import $ from 'jquery-deferred';
// import xtemplate from 'xtemplate';
// import template from './index.html';
// import './index.scss';

export default class Button {
	render(node) {
		// const text = $(node).text();
		
		// $(node).html(
		// 	xtemplate.render(template, {text})
		// );
		alert();
	}
}